<!DOCTYPE html>
  <head>
    <title>{{nameApplication}} :: {{title_createReleaseNote}}</title>
    {{> ../partials/common_head }}
  </head>
  <body>
  {{> ../partials/header }}
  <div class="sticky-top">
      <div class="container-fluid" id="releaseNoteInfo">
        {{> ../partials/release_note_info }}
      </div>
      <div class="container-fluid" id="appTags">
        {{> ../partials/release_note_app_tags}}
      </div>
  </div>
  <div class="container-fluid py-3">
    <div class="col-md-10 offset-md-1 align-self-center" id="cardContainer">
      <!-- see assets/card.html -->
      {{#each sampleReleaseCards}}
        {{> ../partials/release_note_card}}
      {{/each}}
    </div>
    <!--
    <div class="row">
      <div class="col-sm-11 text-right">
        <button type="button" class="btn btn-outline-primary" id="addNote">Add Note <i class="fa fa-plus-circle"></i></button>
      </div>
    </div>
    -->
  </div>
  </body>
</html>
